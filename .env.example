# Example .env for local development
# Copy this to a file named .env and fill the values.

# PostgreSQL-compatible connection string (Neon, Supabase, local postgres)
# Example for local postgres with user `postgres`, password `password`, database `finconnect`:
# DATABASE_URL=postgres://postgres:password@127.0.0.1:5432/finconnect

# JWT secret used to sign tokens. Set this in Vercel as an environment variable (JWT_SECRET).
# You may also set SESSION_SECRET for backward compatibility but JWT_SECRET is preferred.
# Example (generate a random 32+ character secret):
# JWT_SECRET=replace-with-a-long-random-string
# SESSION_SECRET=change-me


# Optional: Port the server should bind to (defaults to 5000)
# PORT=5000
